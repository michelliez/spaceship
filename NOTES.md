# June 3
- .gitignore: to exclude build artifacts, add certain artifacts into the .gitignore file
-  i.e. noticed that hydra would create output artifacts that cluttered up my projects, so added outputs/ & multirun/ to .gitignore

